<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Page – LoveConnect</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(120deg, #fceabb, #f8b500);
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
      }

      .container {
        background: white;
        padding: 2rem;
        border-radius: 16px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
        max-width: 500px;
        width: 100%;
        text-align: center;
      }

      h1 {
        font-size: 2rem;
        color: #e63946;
        margin-bottom: 1.5rem;
      }

      .buttons {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        margin-top: 1.5rem;
      }

      .btn {
        padding: 0.7rem;
        background-color: #e63946;
        color: white;
        border: none;
        border-radius: 8px;
        font-size: 1rem;
        cursor: pointer;
        text-decoration: none;
        transition: background 0.3s;
      }

      .btn2 {
        margin: auto;
        width: 20%;
        padding: 0.7rem;
        background-color: #e63946;
        color: white;
        border: none;
        border-radius: 8px;
        font-size: 1rem;
        cursor: pointer;
        text-decoration: none;
        transition: background 0.3s;
      }

      .btn:hover {
        background-color: #d62839;
      }

      .btn.secondary {
        background-color: #6c757d;
      }

      .btn.secondary:hover {
        background-color: #495057;
      }

      form {
        margin: 0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>My Page</h1>

      <div class="buttons">
        <a class="btn" href="/form/edit">✏️ Edit My Profile</a>
        <a class="btn" href="/invites">💌 View My Invites</a>
        <a class="btn secondary" href="/">⬅ Back to Main Page</a>
        <form method="POST" action="/user/logout" class="btn2">
          <a href="/login" class="btn2" type="submit">🚪 Logout</a>
        </form>
      </div>
    </div>
  </body>
</html>
